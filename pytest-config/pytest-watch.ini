[pytest]
addopts =
        -v
        -W ignore::DeprecationWarning
        -f 

norecursedirs = examples

looponfailroots = 
        daquiri
        tests

qt_api=pyqt5

env=
        QT_QPA_PLATFORM=offscreen